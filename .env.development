# Prediction / history JSON still on GitHub Pages
VITE_DATA_BASE_URL=https://onlyzoran.github.io/win-predict-ai-data/data

# Tournament manifest from admin API (SQLite) — production host (VPS IP /api no longer served)
VITE_LEAGUES_URL=https://win-predict-ai.com/api/leagues.json

# Sports catalog from admin API (enabled only, sorted by sortOrder)
VITE_SPORTS_URL=https://win-predict-ai.com/api/sports
