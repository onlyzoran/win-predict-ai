# Prediction / history JSON still on GitHub Pages
VITE_DATA_BASE_URL=https://onlyzoran.github.io/win-predict-ai-data/data

# Tournament manifest from admin API (same origin as the VPS-hosted frontend)
# Root-relative path → http://202.71.15.138/api/leagues.json (avoids Mixed Content)
VITE_LEAGUES_URL=/api/leagues.json

# Sports catalog from admin API (same origin as the VPS-hosted frontend)
VITE_SPORTS_URL=/api/sports
