import { IconLogoA } from '@onlyzoran/win-predict-ai-icons'

/**
 * Product logo in the app shell. Uses recommended variant A from icons#29 until
 * canonical `IconLogo` is exported after human choice on the Goal.
 */
export const AppLogo = IconLogoA
